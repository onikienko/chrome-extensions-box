var storage = {
    // `sync` if you want. remember about QUOTAS https://developer.chrome.com/extensions/area.html#properties
    area: chrome.storage.local,
    default_options: {
        // one key for every option
    }
};